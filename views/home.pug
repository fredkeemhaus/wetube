extends layouts/main
include mixins/videoBlock

block content
    .videos
        each items in videos
            +videoBlock({
                videoFile:items.videoFile,
                title:items.title,
                views:items.views,
            })